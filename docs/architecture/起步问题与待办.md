# 起步问题与待办

## 当前已经处理好的

- API 已统一到 `/api/v1`
- 分页响应已统一成 `PageResponse`
- 老师原始资料已归档到 `docs/teacher-materials`
- 课程文档草稿已归档到 `docs/course`
- MySQL 最小表设计和 DDL 草案已单独分类

## 当前还存在的起步问题

### P1

- 还没有真实 MySQL 连接验证
- 还没有正式建库和插入测试数据
- 还没有文件上传接口，图片目前只是 URL 字符串

### P2

- 还没有登录鉴权和角色权限控制
- 还没有前端页面
- 还没有单元测试和集成测试

### P3

- Java 实体当前比建议的 MySQL 设计更简化
- 图片正式落库时应从 `repair_requests.image_urls` 升级为独立表
- 宿舍正式落库时建议拆 `dorm_buildings` 和 `dorm_rooms`

## 建议下一步

1. 数据库同事先按 `sql/mysql/01_init_schema_v1.sql` 建库
2. 后端把实体从“简化版”对齐到“正式版”
3. 前端按 `docs/api/接口清单.md` 起页面
4. 补登录、上传、测试
