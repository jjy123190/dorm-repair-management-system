# 起步问题与待办

## 当前已经处理好的

- 已从旧 `Spring Boot REST` 骨架切到 `JavaFX + MyBatis + MySQL` 桌面端主线
- 已完成统一登录、学生注册、手机号找回密码和个人中心改密
- 已完成学生报修、历史时间线、补图、确认完成、申请返修、提交评价主链
- 已完成管理员派单、维修员处理、宿舍目录维护、账号管理、审计日志和月度统计
- 已完成 MyBatis 数据源、Mapper XML、Service 事务模板
- 已提供 `sql/mysql/01_init_schema_v1.sql` 与 `sql/mysql/02_demo_seed_v1.sql`
- 已补齐本地图片存储方案，现场图片与完工凭证都落到项目 `pics/`
- 已形成课程报告、需求说明、架构说明和协作文档

## 当前仍需持续收口的问题

### P1

- 课程答辩材料还缺少成品图示，例如 ER 图、界面截图、流程截图和 PPT
- 个别课程文档需要持续跟进真实实现，避免再次出现旧 Web 口径或“未实现”描述

### P2

- Demo seed 目前已能支撑三类角色演示，但仍可继续补更丰富的返修、待确认、审计场景
- `sql/mysql/01_init_schema_v1.sql` 的中文注释存在历史编码问题，后续如重写 DDL 说明时应统一用正常 UTF-8

### P3

- 当前图片仍走本地 `pics/` 存储，若未来扩展正式部署版本，再考虑云存储
- 统计模块当前聚焦在课程演示所需的查询与图表，不继续扩导出链路

## 建议下一步

1. 先把课程报告、ER 图、关键截图和答辩 PPT 整理成最终提交材料
2. 再根据老师展示顺序补充更完整的 demo seed 场景，保证三类角色切换演示自然
3. 如果还有余力，再修正 DDL 文件里的中文注释编码，提升最终文档观感
